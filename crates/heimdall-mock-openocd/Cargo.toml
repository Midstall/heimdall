[package]
name = "heimdall-mock-openocd"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
description = "In-process OpenOCD Tcl RPC stand-in for Heimdall driver tests. Sized as its own crate so shared test helpers compile once rather than once per integration test binary."

[dependencies]
tokio = { workspace = true, features = [
  "io-util",
  "net",
  "sync",
  "rt",
  "macros",
] }
